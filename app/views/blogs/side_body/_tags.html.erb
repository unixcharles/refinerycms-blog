<% unless (tags = Blog.tags).blank? %>
  <h2>Tags</h2>
  <ul id="tags">
    <% tags.each do |tag| %>
      <li>
        <%= link_to tag, blog_tag_url(tag) -%>
      </li>
    <% end %>
  </ul>
<% end %>